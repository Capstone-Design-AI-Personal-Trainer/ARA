# Pose Player

GT pose frames rendered as a filled human silhouette.

Responsibilities:
- load GT pose JSON
- play frames on a time axis
- project normalized pose points into canvas space
- render a human-like silhouette instead of a skeleton

Primary entry points:
- `PoseSilhouetteCanvas.jsx`
- `index.js`
